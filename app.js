document.addEventListener('DOMContentLoaded',()=>{const body=document.body;const theme=document.querySelector('[data-theme]');const menu=document.querySelector('[data-menu]');const links=document.querySelector('[data-links]');const top=document.querySelector('.to-top');const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;if(localStorage.getItem('rodnik-theme')==='dark')body.classList.add('dark');theme?.addEventListener('click',()=>{body.classList.toggle('dark');localStorage.setItem('rodnik-theme',body.classList.contains('dark')?'dark':'light')});menu?.addEventListener('click',()=>{links?.classList.toggle('open');menu.setAttribute('aria-expanded',links?.classList.contains('open')?'true':'false')});const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));window.addEventListener('scroll',()=>top?.classList.toggle('show',scrollY>500),{passive:true});top?.addEventListener('click',()=>scrollTo({top:0,behavior:reduced?'auto':'smooth'}));document.querySelectorAll('[data-type]').forEach(el=>{const text=el.textContent;el.textContent='';let i=0;const tick=()=>{if(i<text.length){el.textContent+=text[i++];setTimeout(tick,42)}};setTimeout(tick,250)})});