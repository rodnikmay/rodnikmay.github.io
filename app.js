document.addEventListener('DOMContentLoaded',()=>{const body=document.body,theme=document.querySelector('[data-theme]'),menu=document.querySelector('[data-menu]'),links=document.querySelector('[data-links]'),top=document.querySelector('.to-top'),reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(localStorage.getItem('rodnik-theme')==='dark')body.classList.add('dark');theme?.addEventListener('click',()=>{body.classList.toggle('dark');localStorage.setItem('rodnik-theme',body.classList.contains('dark')?'dark':'light')});menu?.addEventListener('click',()=>{links?.classList.toggle('open');menu?.setAttribute('aria-expanded',links?.classList.contains('open')?'true':'false')});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));addEventListener('scroll',()=>top?.classList.toggle('show',scrollY>450),{passive:true});top?.addEventListener('click',()=>scrollTo({top:0,behavior:reduced?'auto':'smooth'}));document.querySelectorAll('[data-count]').forEach(el=>{const target=Number(el.dataset.count),start=performance.now();const run=now=>{const p=Math.min((now-start)/1100,1);el.textContent=Math.floor((1-Math.pow(1-p,3))*target).toLocaleString('ru-RU');if(p<1)requestAnimationFrame(run)};if(reduced)el.textContent=target.toLocaleString('ru-RU');else requestAnimationFrame(run)});document.querySelectorAll('[data-tilt]').forEach(card=>{card.addEventListener('pointermove',e=>{if(reduced)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(800px) rotateX(${y*-2}deg) rotateY(${x*2}deg) translateY(-4px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});const prompts=['Напиши четыре строки о городе у горной реки.','Опиши утро в Майском через звук воды.','Сравни память с родником и найди неожиданный образ.','Сочини начало стихотворения о дороге домой.','Передай характер человека через пейзаж Кавказа.'];document.querySelectorAll('[data-prompt]').forEach(btn=>btn.addEventListener('click',()=>{const out=document.querySelector(btn.dataset.prompt);if(out)out.textContent=prompts[Math.floor(Math.random()*prompts.length)]}));document.querySelectorAll('[data-prompt-type]').forEach(btn=>btn.addEventListener('click',()=>{const el=document.querySelector(btn.dataset.promptType),types=['Тема для стихотворения','Образ','Первая строка'];if(el){el.dataset.mode=String((Number(el.dataset.mode||0)+1)%types.length);el.textContent=types[Number(el.dataset.mode)]}}));document.querySelectorAll('[data-type]').forEach(el=>{const text=el.textContent;el.textContent='';let i=0;const tick=()=>{if(i<text.length){el.textContent+=text[i++];setTimeout(tick,35)}};setTimeout(tick,220)})});