document.addEventListener('DOMContentLoaded',()=>{const body=document.body,theme=document.querySelector('[data-theme]'),menu=document.querySelector('[data-menu]'),links=document.querySelector('[data-links]'),top=document.querySelector('.to-top');const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(localStorage.getItem('rodnik-theme')==='dark')body.classList.add('dark');theme?.addEventListener('click',()=>{body.classList.toggle('dark');localStorage.setItem('rodnik-theme',body.classList.contains('dark')?'dark':'light')});menu?.addEventListener('click',()=>{links?.classList.toggle('open');menu.setAttribute('aria-expanded',links?.classList.contains('open')?'true':'false')});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));addEventListener('scroll',()=>top?.classList.toggle('show',scrollY>450),{passive:true});top?.addEventListener('click',()=>scrollTo({top:0,behavior:reduced?'auto':'smooth'}));document.querySelectorAll('[data-count]').forEach(el=>{const target=Number(el.dataset.count),start=performance.now();const run=now=>{const p=Math.min((now-start)/1200,1);el.textContent=Math.floor((1-Math.pow(1-p,3))*target);if(p<1)requestAnimationFrame(run)};if(!reduced)requestAnimationFrame(run);else el.textContent=target});document.querySelectorAll('[data-tilt]').forEach(card=>{card.addEventListener('pointermove',e=>{if(reduced)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(700px) rotateX(${y*-3}deg) rotateY(${x*3}deg) translateY(-5px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});document.querySelectorAll('[data-type]').forEach(el=>{const text=el.textContent;el.textContent='';let i=0;const tick=()=>{if(i<text.length){el.textContent+=text[i++];setTimeout(tick,38)}};setTimeout(tick,250)})});